# Template TODO

This is a starting worklist. Replace it with project-specific tasks after
writing the project plan. Keep every task self-contained.

- [ ] State the project’s current object, authority, scope, and unresolved paths in `PLAN.md`.
- [ ] Create the first bounded task with acceptance criteria, dependencies, and file scope.
- [ ] Register the worker identity that will claim the task.
- [ ] Run the task acceptance command and record its evidence with `fw receipt test`.
- [ ] Add a review step before expanding completion policy.
- [ ] If Beads is needed, inspect the installed `br` contract before running `br init`.
- [ ] If external effects are added, define intent, capability, freshness, observation, verification, and receipt requirements first.
